#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    int largestSumAfterKNegations(vector<int>& A, int K)
    {
        int n = A.size(); 
        if(n == 0)
        sort(A.begin(), A.end());
        int ans = 0;
        for (int i = 0; i < K; ++i) {
            
        
        }
    }
};