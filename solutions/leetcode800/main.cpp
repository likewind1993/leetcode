#include <string>
#include <iostream>
using namespace std;

class Solution {
public:
    int getSimilarity(string str1, string str2) {
        return ((stoi(str1.substr(0,2)-stoi(str2.substr()))
    }
    string similarRGB(string color)
    {

    }
};